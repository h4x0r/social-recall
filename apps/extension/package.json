{
  "name": "@social-recall/extension",
  "version": "0.0.5",
  "description": "Chrome extension for Social Recall - save LinkedIn profiles with one click",
  "scripts": {
    "build": "npx esbuild src/content.ts src/background.ts src/popup.ts src/settings.ts --bundle --outdir=dist --target=es2017 --format=iife",
    "watch": "npx esbuild src/content.ts src/background.ts src/popup.ts src/settings.ts --bundle --outdir=dist --target=es2017 --format=iife --watch",
    "dev": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@vitest/coverage-v8": "^4.0.16",
    "esbuild": "^0.24.0",
    "jsdom": "^27.3.0",
    "typescript": "^5.5.4",
    "vitest": "^4.0.16"
  }
}
